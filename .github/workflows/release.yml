name: Legacy release entry point (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Use the protected desktop release workflow
        run: |
          echo "Direct legacy publication is disabled."
          echo "Run 'Release VoiceLab Desktop' with an existing version tag."
          exit 1
